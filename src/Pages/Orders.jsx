const Orders = () => (
    <div>
    <h2>Orders</h2>
    <p>No new orders.</p>
    <button onClick={() => alert('Checking for new orders...')}>Check Orders</button>
    </div>
);

export default Orders;
